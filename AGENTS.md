# Repository Working Agreement

本文件约束所有人类与 AI 代理在本仓库中的工作方式。

## 开始任务前

1. 阅读 `README.md`、`Docs/WORKFLOW.md` 和与任务相关的 RFC/ADR。
2. 检查当前文件状态，保留不属于本任务的修改。
3. 明确任务卡中的目标、非目标、验收标准与验证命令；缺失时先补规格。
4. 一次任务只解决一个可独立验证的问题。不要顺手重构无关区域。

## 架构边界

- `Game.Core`：纯 C# 领域规则；不得引用 Unity。
- `Game.Foundation`：跨模块契约和稳定配置。
- `Game.Gameplay`：用例编排与玩法模块；依赖 Core/Foundation。
- `Game.Presentation`：视图、动画、音频、输入呈现；领域状态不放在这里。
- `Game.Infrastructure`：存档、Addressables、平台、场景等适配器。
- 内容必须通过稳定 ID 引用；存档不得依赖 Unity 实例 ID、资源路径或显示文本。

新增跨层依赖或第三方包前必须提交 ADR。不得让 Core 引用 Unity API。

## 实现规则

- 优先做最小端到端切片，再扩展内容数量。
- 规则逻辑保持确定性；随机行为必须注入种子并可重放。
- 玩家可见文本进入本地化表，不硬编码在组件或规则中。
- 变更存档结构时提升 schema 版本并提供迁移和回归测试。
- 生成内容必须经过 schema、引用完整性、平衡边界和本地化覆盖校验。
- 禁止提交密钥、个人数据、模型凭据、未确认授权的素材。

## 完成定义

任务只有在以下条件全部满足时才完成：

- 验收标准逐条可证明。
- 新增/修改规则具有 EditMode 测试；关键玩家路径具有 PlayMode 或冒烟测试。
- `pwsh Tools/Workflow.ps1 check` 通过。
- 没有新增 Console error、失效引用或未解释警告。
- 文档、内容 schema、迁移说明与实际行为同步。
- 交付说明包含修改范围、验证证据、已知风险和回滚方式。

## AI 输出要求

AI 不得把“已生成代码”视为完成。报告必须区分：已验证事实、合理推断、尚未验证项。任何影响玩法数值、叙事设定、美术方向或发行范围的主观决定，都应写入 RFC 并由负责人确认。
